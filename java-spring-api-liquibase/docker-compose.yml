version: '3.1'

services:
  db:
    image: mariadb
    restart: always
    environment:
      MARIADB_USER: root
      MARIADB_PASSWORD: root
      MARIADB_DATABASE: springapiliquibasedb
      MARIADB_ROOT_PASSWORD: root
    ports:
      - 3306:3306